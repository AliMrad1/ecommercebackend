
server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ecommerce
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  main:
    web-application-type: servlet

encryption:
  salt:
    rounds: 10

jwt:
  algorithm:
    key: SpringJwtComplex
  issuer: ecommerce
  expiredInSeconds: 604800

mail:
  host: smtp.office365.com
  port: 587
  username: pr.alimrad@outlook.com
  password: 01792385@Oo

  properties:
    mail:
      smtp:
        ssl:
          trust: "*"
        auth: true
        starttls:
          enable: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000
